<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.koh1project.SplitTheCost">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/drink1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/drink1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SimpleCalculationActivity" />
        <activity android:name=".CalPrefsActivity" />
        <activity android:name=".ManageListActivity" />
        <activity android:name=".AddMemberActivity" />
        <activity android:name=".MemberListActivity" />
        <activity android:name=".EditMemberActivity" />
        <activity android:name=".GroupActivity" />
        <activity android:name=".GroupListActivity" />
        <activity android:name=".PositionListActivity" />
        <activity android:name=".SpecialCalculationActivity" />
        <activity android:name=".CheckListActivity" />
        <activity android:name=".AddParticipantsActivity"></activity>
    </application>

</manifest>